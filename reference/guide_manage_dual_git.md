# DUAL GIT PROCESS

## PERSONAL (mohdazlanabas)

cd path/to/personal-repo
git config user.name "Azlan"
git config user.email "mohdazlan@gmail.com"

# Clone repository
git clone git@github.com:mohdazlanabas/repo-name.git

# Test SSH connection
ssh -T git@github.com-mohd

# GitHub username: mohdazlanabas

---

## WORK - GAIA (Gaia-Digital-Agency)

cd path/to/work-repo
git config user.name "net1io"
git config user.email "azlan@net1io.com"

# Clone repository
git clone git@github.com-gaia:Gaia-Digital-Agency/repo-name.git

# Test SSH connection
ssh -T git@github.com-gaia

# GitHub username: Gaia-Digital-Agency (Organization)

---

## WORK - AZLANET (azlanet1io)

cd path/to/azlanet-repo
git config user.name "azlanet"
git config user.email "azlan@net1io.com"

# Clone repository
git clone git@github.com-azlanet:azlanet1io/repo-name.git

# Test SSH connection
ssh -T git@github.com-azlanet

# GitHub username: azlanet1io